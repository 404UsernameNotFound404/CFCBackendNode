const organizationModel = require('../db/models').organization;\
const organizationModelDetails = require("../organizationModel");

const createOrgs = () async => {
    const createdOrgs = [];
    organizationModelDetails.allowedEntries.map(ele => {
        
    })
}